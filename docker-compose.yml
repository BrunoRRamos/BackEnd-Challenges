services:
  database:
    image: postgres
    container_name: commom
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=commom_db
    volumes:
      - pgdata:/data/postgres

volumes:
  pgdata:
    driver: local